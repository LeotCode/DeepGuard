# DeepGuard Backend — Python dependencies
#
# ── Python version ─────────────────────────────────────────────────────────
# Python 3.9, 3.10, or 3.11 ONLY.
# TensorFlow has no wheels for Python 3.12+ on ANY platform.
# Check your version: python --version
# If wrong, recreate your venv:
#   python3.11 -m venv .venv && source .venv/bin/activate
#
# ── Platform notes ─────────────────────────────────────────────────────────
# macOS Apple Silicon (M1/M2/M3/M4): ✅ all packages have ARM wheels
# macOS Intel:                        ✅ all packages supported
# Windows x86-64:                     ✅ all packages supported
# Linux x86-64:                       ✅ all packages supported
# Linux ARM64 (Raspberry Pi etc):     ⚠️  no TF wheel — build from source

# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Firebase ───────────────────────────────────────────────────────────────
firebase-admin>=6.5.0

# ── PyTorch ────────────────────────────────────────────────────────────────
# CPU build installs on all platforms automatically.
# For GPU (CUDA), install manually first: https://pytorch.org/get-started/locally/
torch>=2.2.0
torchvision>=0.17.0

# ── TensorFlow ─────────────────────────────────────────────────────────────
# 2.15.x = last version with tensorflow.keras (Keras 2 API).
# 2.16+ switched to standalone Keras 3, breaking tf.keras.applications.*.
# Wheels exist for: macOS ARM, macOS x86, Windows x86, Linux x86.
# Python 3.9 / 3.10 / 3.11 only — no 3.12+ wheels exist for ANY TF version.
tensorflow>=2.15.0,<2.16.0

# tensorflow-hub: compatible with TF 2.15
tensorflow-hub>=0.16.0

# ── HDF5 ───────────────────────────────────────────────────────────────────
# Required for loading .h5 and .keras model weight files directly.
# TF 2.15 uses h5py internally; we also use it directly in model_runner.py.
h5py>=3.10.0,<4.0

# ── Vision / image processing ──────────────────────────────────────────────
Pillow>=10.0
# headless = no Qt/GTK GUI deps; correct for server use on all platforms
opencv-python-headless>=4.9.0

# ── Numeric ────────────────────────────────────────────────────────────────
# numpy 2.x breaks TF 2.15 — must stay on 1.x
numpy>=1.26,<2.0

# ── Utilities ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.7.0
certifi>=2024.0.0